# Public URL of the Vocs documentation site (apps/docs). Falls back to the
# GitHub README when unset. For local development, run `pnpm --filter docs dev`
# and point this at that server (default http://localhost:5173).
VITE_DOCS_URL=http://localhost:5173
